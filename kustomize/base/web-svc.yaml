apiVersion: v1
kind: Service
metadata:
  name: words-web-svc
  namespace: words-demo
  labels:
    app: words-web
spec:
  ports:
    - port: 80
      targetPort: 80
      name: words-web
  selector:
    app: words-web
